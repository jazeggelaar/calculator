    if page == 3:
        st.write("[parent]:")
        st.write("| [string]")
        if st.button("extra information"):
            page_wanted = 4
        if st.button("back"):
            page_wanted = 2
    if page == 4:
        st.write(": as /")
        st.write("x as *")
        if st.button("back"):
            page_wanted = 3
    if page == 5:
        st.write("unsupported = not allowed")
        st.write("supported = allowed")
        if st.button("back"):
            page_wanted = 2